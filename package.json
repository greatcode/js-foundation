{
  "name": "js-foundation",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "main": "src/api/server.js",
  "scripts": {
    "start": "NODE_ENV=dev ./node_modules/.bin/nodemon --watch src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^5.3.2",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "connect-livereload": "^0.6.1",
    "livereload": "^0.9.3",
    "nodemon": "^2.0.7"
  }
}
